# Test fixtures

Only add small audio files whose redistribution rights are documented. Each fixture must have an expected metadata record under `metadata/` and a SHA-256 entry in `SHA256SUMS.txt`. Tests that scan fixtures must confirm all hashes remain unchanged.
