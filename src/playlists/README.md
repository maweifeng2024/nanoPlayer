# Playlists feature

Current playlist UI is implemented in `../library/ContentPage.tsx`, `../library/TrackTable.tsx`, `../library/playlistCover.ts` and `../store.ts`; this directory currently documents ownership rather than containing the implementation. Creation, ordering, membership and custom/automatic artwork exist. M3U8 import/export remains unimplemented. Source audio deletion never belongs in this feature.
