# Linux packaging

2026-09-11：v0.1.7 已发布 x64 AppImage、DEB、RPM。下述发行版、音频设备和 MPRIS 检查为验收要求；当前无专用原生 MPRIS 实现。

Produce AppImage and Debian packages from native Linux runners. Document required WebKitGTK and audio packages per distribution. Validate PipeWire and ALSA output, MPRIS controls, non-UTF-8 path errors, removable volumes, desktop entry registration, and package removal without deleting user music or the database unless the user explicitly removes application data.

